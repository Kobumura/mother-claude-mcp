# Copy to /etc/mother-claude-mcp.env, chown root:root, chmod 0600.
# Generate the token with: openssl rand -hex 32
MOTHER_CLAUDE_TOKEN=replace-me
MOTHER_CLAUDE_CONFIG=/srv/mother-claude-docs/mother-claude.config.json
